<div class="container">
    <div class="row">
        {{#if user.admin}}

        {{#if client}}

            <div class="col-lg-12">
                <small>DAtos de cliente</small>
                
            </div>
        
        {{else}}
        <p>Pudo haber ocurrido un error</p>
{{/if}}



{{else}}

<h4>Usted no debería estar aquí</h4>

{{/if}}
    </div>
</div>